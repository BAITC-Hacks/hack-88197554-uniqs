# TODO

Одна строка = одна задача = один владелец. Только то, что нужно для DEMO.md. А — мозг, Б — экраны.

## Сейчас (максимум 1 на человека)
- [ ] (А) F0 Скелет по `prompts/skeleton.md`, один агент, до всех остальных: S1 — приложение, данные, контракт, заглушки API; S2 — оболочка 3D-города и HUD. После скелета режем на фичи по списку из VISION.
- [ ] (Б, руками, пока агенты кодят) Выбрать одно CC0-семейство low-poly моделей (Kenney / KayKit / Quaternius, лицензию проверить): офисная башня (одна модель, 8 цветов), 7 площадок (таблица в VISION), юрта, дома, персонаж с idle/run, NPC, деревья и декор. GLB → `public/models/`, список файл → здание.
- [ ] (Б) Фикстуры `Profile`, `Recommendation[]` и `ProgressDelta` для E0028 под моки UI.

## Дальше
- [ ] (А) F1 engine, порт 3001: эффективные навыки (доначисление после `last_review_date`), цель, разрывы, фильтр доступных активностей, скоринг с факторами, `completeActivity → ProgressDelta`. Эндпоинты `/api/profile/[id]`, `/api/recommend/[id]`, `/api/progress`. Проверка на E0028, E0072, E0052, E0026.
- [ ] (А) F2 mentor, порт 3002: агентный цикл с инструментами из engine, SSE-стрим `AgentStep`, панель наставника с шагами и карточками. Сначала мок LLM, потом реальный провайдер.
- [ ] (Б) F3 character + venues, порт 3003: лист персонажа (навыки против требований, критичные, история, «+1 после ревью»), панели площадок и журнал квестов с «Выполнено» / «Не сейчас». До готовности engine — на фикстурах.
- [ ] (Б) F4 hr, порт 3004: проседающие навыки по отделам, «нет шага», участие по активностям.
- [ ] (Б) F5 upload, порт 3005: загрузка JSON/CSV в формате датасета и слияние со store.
- [ ] Фичи города после скелета: city-life («!» над площадками, подсказка пути, «+1» и лифт по `ProgressDelta`, NPC, облачка наставника), city-assets (GLB вместо примитивов, свет), extras (пасхалки P3). Стоп-критерии — в VISION.
- [ ] (А+Б) Последний час: README (методология рекомендации, запуск одной командой), репетиция по DEMO.md, видео-fallback.
- [ ] Уточнить у организаторов: можно ли коммитить `data/` в репо. В ТЗ сказано, что данные за пределы хакатона не выносятся.

## Сделано

## Не делаем
Voice Router (кейс 2), локализация kk/ru, мобильная версия, внутренняя валюта и магазин наград, публичные рейтинги, настоящая auth (роль — переключатель в шапке), БД.
